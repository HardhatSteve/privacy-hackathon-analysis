# VeilClient

The main entry point for the SDK.

## Constructor

```typescript
new VeilClient(config: VeilConfig)
```

## Methods

### `initialize()`
Sets up connections.

### `getAdapter(name)`
Returns a specific adapter instance.
