# Example environment configuration for sapp-backend

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/sapp

# Server port
PORT=4002

# Node environment
NODE_ENV=development

# ===========================================
# Privy Configuration (Required)
# Get these from https://dashboard.privy.io/
# ===========================================

# Your Privy App ID
PRIVY_APP_ID=your-privy-app-id

# Your Privy App Secret (keep this secure!)
PRIVY_APP_SECRET=your-privy-app-secret

# Optional: Authorization key for app-controlled wallets
# Only needed if using app-owned wallets instead of user-owned
# PRIVY_AUTHORIZATION_KEY=your-authorization-private-key

# ===========================================
# SilentSwap Configuration
# ===========================================
# Note: SilentSwap uses user signatures from Privy embedded wallets, not backend keys

# SilentSwap Environment (STAGING or MAINNET)
SILENTSWAP_ENVIRONMENT=STAGING

# SilentSwap API Base URL
SILENTSWAP_API_URL=https://api-testnet.silentswap.com

# Solana RPC URL (devnet for testing, mainnet-beta for production)
SOLANA_RPC_URL=https://api.devnet.solana.com
